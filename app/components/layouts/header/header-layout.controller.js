/**
 * @ngdoc controller
 * @name tsHeader.HeaderCtrl
 * @class
 *
 * @description Controller for header component
 */
export default class HeaderCtrl {
  onLoad(src) {
    this.onLoadFile({ src });
  }
}